if [ -n "$(go vet ./...)" ]; then
    echo "go vet failed."
    exit 1
fi

if [ -n "$(go build ./...)" ]; then
    echo "go build failed."
    exit 1
fi

if [ -n "$(go fmt ./...)" ]; then
    echo "go build failed."
    exit 1
fi
